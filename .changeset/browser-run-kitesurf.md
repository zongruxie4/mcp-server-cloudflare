---
'cloudflare-browser-mcp-server': minor
---

Add an optional `browser` argument to the Browser Run page tools. Set it to `kitesurf` to render the page with the Kitesurf browser engine. Quick actions send it as the `?browser=` query param. `start_crawl` sends it in the request body.
