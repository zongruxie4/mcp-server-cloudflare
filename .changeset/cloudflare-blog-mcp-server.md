---
"cloudflare-blog": minor
---

Add the Cloudflare Blog MCP server (`blog.mcp.cloudflare.com`). Exposes tools to search posts via semantic search, list posts (with tag filtering and pagination), get a single post by slug, and list all tags.
