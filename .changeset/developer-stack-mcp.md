---
"stack-mcp": patch
---

Release the Developer Stack MCP server: a documentation-search MCP backed by Cloudflare AI Search that exposes a curated Cloudflare developer stack. Provides `search_docs` and `list_libraries` tools, with per-request scoping via the `?libs=` URL param.
